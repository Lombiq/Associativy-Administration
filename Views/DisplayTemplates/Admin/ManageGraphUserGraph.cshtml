@model Associativy.Administration.Models.Admin.AssociativyManageGraphUserGraphPart
           
@if (Model.IsUserGraph)
{
    <div>
        <h3>@T("User Graph")</h3>
        @Html.Link(T("Edit graph").Text, Url.ItemEditUrl(Model.UserGraph))
    </div>
}